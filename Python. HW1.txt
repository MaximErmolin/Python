# Python. HW_1:
variable_String = str("s1") #1) —оздать переменную типа String
variable_Integer = int(1) # 2) —оздать переменную типа Integer
variable_Float = float(2.8)# 3) —оздать переменную типа Float
variable_Bytes = bytes(1)# 4) —оздать переменную типа Bytes
variable_List = [1, 33, 6, 9]# 5) —оздать переменную типа List
variable_Tuple = (36.6, 'Normal', None, False)# 6) —оздать переменную типа Tuple
variable_Set = {1, 2, 3, 4, 5, 6} # 7) —оздать переменную типа Set
variable_Frozen_set = frozenset(variable_List)# 8. —оздать переменную типа Frozen set
variable_Dict = {'dict': 1, 'dictionary': 2}# 9) —оздать переменную типа Dict
print("переменна¤ типа String:", type(variable_String))# 10) ¬ывести в консоль все выше перечисленные переменные с добавлением типа данных.
print("переменна¤ типа Integer:", type(variable_Integer))#
print("переменна¤ типа Float:", type(variable_Float))
print("переменна¤ типа Bytes:", type(variable_Bytes))
print("переменна¤ типа List:", type(variable_List))
print("переменна¤ типа Tuple:", type(variable_Tuple))
print("переменна¤ типа Set:", type(variable_Set))
print("переменна¤ типа Frozen set:", type(variable_Frozen_set))
print("переменна¤ типа Dict:", type(variable_Dict))

StringForSum1 = str("sum1")
StringForSum2 = str("sum2")
StringForSum1Sum2 = StringForSum1 + StringForSum2
print("переменна¤ в которой суммируютс¤ String:",StringForSum1Sum2)
# 11) —оздать 2 переменные String, создать переменную в которой суммируете эти переменные. ¬ывести в консоль.

IntForSum1 = int(2)
IntForSum2 = int(1)
IntForSum1Sum2 = IntForSum1 + IntForSum2
print("переменна¤ в которой суммируютс¤ Integer:",IntForSum1Sum2 )
# 12) —оздать 2 переменные Integer, создать переменную в которой суммируете эти переменные. ¬ывести в консоль.

IntForDiv1 = int(28)
IntForDiv2 = int(3)
IntForDiv1Div2 = IntForDiv1 / IntForDiv2
print("переменна¤ в которой дел¤тс¤ Integer:", IntForDiv1Div2)
# 13) —оздать переменную в которой –азделите эти переменные Int. ¬ывести в консоль.

IntForMul1 = int(5)
IntForMul2 = int(4)
IntForMul1Mul2 = IntForMul1 * IntForMul2
print("переменна¤ в которой умножаютс¤ Integer:", IntForMul1Mul2)
# 14) —оздать переменную в которой ”множите эти переменные Int. ¬ывести в консоль.

IntForDivF1DivF2 = IntForDiv1 // IntForDiv2
print("переменна¤ в которой дел¤тс¤ Integer без остатка:", IntForDivF1DivF2)
# 15) —оздать переменную в которой –азделите без остатка эти переменные Int. ¬ывести в консоль.

IntForDiv1Div2eDivF1DivF2 = IntForDiv1Div2 - IntForDivF1DivF2
print("переменна¤ в которой надо присвоить остаток от делени¤ этих переменных Int:", IntForDiv1Div2eDivF1DivF2)
# 16) —оздать переменную в которой надо присвоить остаток от делени¤ этих переменные Int. ¬ывести в консоль.

arrr = (7 + 12) **3 + 7 * 4 - 44 / 2 **4
print(arrr)
# 17) (7 + 12)  3 + 7 * 4 - 44 / 2  4 расставить точки так чтобы получилось 6884.25. ¬ывести в консоль.





# «десь записанно все то что мы вводили на уроке с преподователем:
# a = 5
# b = 2
# c = a + b
#
# print('C = ', c)
#
# d = c + 10
# print("D = ", d)
#
# if d > 10:
#
#         print ('D > 10')
#
# print('Next_func')
#
# a = 'Elochka'
# b = ' Zelenoya'
#
# c = a + b
# print(c)
#
# result = a + str(d)
#
# print(result)
#
# print(a*d)
#
# print(a[1]*d)